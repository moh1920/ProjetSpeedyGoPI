/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { Vehicule } from '../../models/vehicule';

export interface GetAllVehicles$Params {
}

export function getAllVehicles(http: HttpClient, rootUrl: string, params?: GetAllVehicles$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<Vehicule>>> {
  const rb = new RequestBuilder(rootUrl, getAllVehicles.PATH, 'get');
  if (params) {
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<Vehicule>>;
    })
  );
}

getAllVehicles.PATH = '/vehicle/getAllVehicles/{id}';
