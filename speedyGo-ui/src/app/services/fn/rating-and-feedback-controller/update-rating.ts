/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { RatingAndFeedback } from '../../models/rating-and-feedback';

export interface UpdateRating$Params {
  id: number;
      body: RatingAndFeedback
}

export function updateRating(http: HttpClient, rootUrl: string, params: UpdateRating$Params, context?: HttpContext): Observable<StrictHttpResponse<RatingAndFeedback>> {
  const rb = new RequestBuilder(rootUrl, updateRating.PATH, 'put');
  if (params) {
    rb.path('id', params.id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<RatingAndFeedback>;
    })
  );
}

updateRating.PATH = '/api/ratings/updateRating/{id}';
